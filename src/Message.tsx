// Pascal Casing
function Message() {
	//JSX - JavaScript XML
	// gets comverted to JS code

	const name = "";
	if (name) return <h1>Hello {name}</h1>;
	else return <h1>Hello Guest</h1>;
}

export default Message; //default export
